ONBUILD <%= Git::import_or_clone(c['ruby.mezuro_app_path'],
                                 'https://github.com/mezuro/mezuro.git') %>

ONBUILD RUN bash -l -c "cd <%= c['ruby.mezuro_app_path'] %> && rvm install $(cat .ruby-version)"
ONBUILD RUN bash -l -c "cd <%= c['ruby.mezuro_app_path'] %> && gem install bundler && bundle install"